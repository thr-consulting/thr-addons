{
	"extends": "../../tsconfig.json",
	"compilerOptions": {
		"declarationDir": "dist",
		"lib": ["esnext", "dom"],
		"jsx": "react"
	},
	"include": [
		"src/index.ts",
		"types/*.d.ts"
	],
	"comments": [{
		"comment":  "Add this to include section above, but remove it before check in",
		"include": [
			"src/**/*.stories.tsx"
		]
	}]
}
